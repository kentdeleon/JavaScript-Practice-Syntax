/*JavaScript Object */

/*JavaScript variables are containers for data values.
Objects are variables too, but they can contain many values.

Think of an object as a list of values that are written as name:value pairs,
with the names and the values separated by colons.

*/

var person={ name:"Kent", age:24, FavColor:"blue"};


document.write(person.name); //first method to access object property
document.write("<br>");
document.write(person['age']); //second method to access object property